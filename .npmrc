# react-simple-maps@3 declares a peer range of React <=18 while this project is
# on React 19. The package works fine at runtime, so accept the mismatch instead
# of blocking every install. Without this, `npm install` and the shadcn CLI both
# fail with ERESOLVE.
legacy-peer-deps=true
